# 按需推送决策（2026-09-07）

本记录更新旧版固定日报的发送要求，栏目范围与基线审批继续有效。

用户确认：保持每 3 小时采集，检测到新增即报告；标题或日期改变的 UPDATED 也推送并标注“更新”；凌晨照常发送；无新增但出现采集故障时允许单独提醒。

实现规则：每轮合并所有待报 NEW、UPDATED 和采集异常；健康且无待报内容时不发送。取消独立固定日报和每日一次限制，沿用生产事件的 reported_at 去重。失败内容保留并在后续轮次补报。持续故障每轮提醒；已成功报告的故障恢复后保持安静，未送达的异常则补报其恢复状态。沿用现有飞书关键词“南开通知日报”，显示本轮采集开始时间。原状态与历史基线不清空。

工作流 crawl.yml 在北京时间 02:42、05:42、08:42、11:42、14:42、17:42、20:42、23:42 运行 watch；daily-report.yml 改为只接受手动触发的兼容入口。两者共享状态并发锁。无新增、更新和异常时，手动入口也不发送空报告。

不增加长期自动运行检查，不增加状态定期备份。GitHub 定时延迟仍可能发生；此次改动消除等待固定日报时点，不承诺准点调度。
